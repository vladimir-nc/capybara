# Automatically generated on 2020-06-15 UTC from http://localhost:3000/app/5ee7d592f3d3d9000b7b9dd6/settings
# Note that this configuration is not an exact match to UI settings. Review and adjust as necessary.

workflows:
  default-workflow:
    name: Default Workflow
    max_build_duration: 30
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
    scripts:
      - flutter packages pub get
    artifacts:
      - flutter_drive.log
    publishing:
      email:
        recipients:
          - remarkov@gmail.com
  tests-on-mac-pro:
    name: Tests on Mac Pro
    instance_type: mac_pro
    max_build_duration: 30
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
    scripts:
      - flutter packages pub get
    artifacts:
      - flutter_drive.log
    publishing:
      email:
        recipients:
          - remarkov@gmail.com
